# Nested agent home file

Here so tests/e2e/agent-home-e2e.bats covers a directory being recreated under the target home
rather than only files at its top level. See ../.rix-e2e-marker for what this fixture is.
